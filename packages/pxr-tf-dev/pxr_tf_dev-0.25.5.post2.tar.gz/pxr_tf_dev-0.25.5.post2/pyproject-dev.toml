[build-system]
requires = [
    "scikit-build-core",
    "pxr-arch-dev==0.25.5",
    "pxr-boost-dev==0.25.5",
    "pxr-tbb-dev==2022.1.0.1"
]
build-backend = "scikit_build_core.build"

[project]
name = "pxr-tf-dev"
version = "0.25.5.post2"
description = "Regroup foundation tool for software development used in OpenUSD"
readme = "README.md"
requires-python = ">=3.8"
license-files = ["LICENSE.txt", "NOTICE.txt"]
keywords = ["pixar", "open-usd", "dev"]
classifiers = [
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Operating System :: POSIX :: Linux",
    "Operating System :: MacOS :: MacOS X",
    "Operating System :: Microsoft :: Windows :: Windows 10",
]
dependencies = [
    "pxr-arch-dev==0.25.5",
    "pxr-boost-dev==0.25.5",
    "pxr-tbb-dev==2022.1.0.1"
]

[project.urls]
Repository = "https://github.com/untwine/pxr-tf"

[tool.scikit-build]
cmake.version = ">=3.21,<4"
cmake.args = [
    "-DBUILD_TESTS=OFF",
    "-DBUILD_SHARED_LIBS=ON",
    "-DBUILD_PYTHON_BINDINGS=ON",
    "-DCMAKE_INSTALL_LIBDIR=pxr-tf/lib",
    "-DCMAKE_INSTALL_INCLUDEDIR=pxr-tf/include",
    "-DCMAKE_INSTALL_DATAROOTDIR=pxr-tf",
    "-DCMAKE_INSTALL_PYTHON_LIBDIR=pxr-tf/lib/python",

    # Aligned delete in TfSingleton::DeleteInstance requires macOS >= 10.13
    "-DCMAKE_OSX_DEPLOYMENT_TARGET=10.13"
]
sdist.exclude = ["test", ".github"]
