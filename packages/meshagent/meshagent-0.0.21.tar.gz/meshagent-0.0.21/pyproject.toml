[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "meshagent"
description = "Meshagent"
requires-python = ">=3.9.0"
license = "Apache-2.0"
keywords = []
dependencies = [
  
]

dynamic = ["version", "readme"]

[project.urls]
Documentation = "https://meshagent.com"
Website = "https://meshagent.com"
Source = "https://github.com/meshagent"

[tool.setuptools.packages.find]
include = ["meshagent"]

[tool.setuptools.package-data]
"meshagent" = ["py.typed", "*.pyi", "**/*.pyi", "**/*.js"]

[tool.setuptools.dynamic]
version = {attr = "meshagent.version.__version__"}
readme = { file = "README.md", content-type = "text/markdown" }


[project.optional-dependencies]
all = [
  "meshagent-api[all]~=0.0.21",
  "meshagent-agents[all]~=0.0.21",
  "meshagent-tools~=0.0.21",
  "meshagent-openai~=0.0.21",
  "meshagent-computers~=0.0.21",
  "meshagent-mcp~=0.0.21",
  "meshagent-livekit~=0.0.21",
  "meshagent-cli~=0.0.21"
]

agents = [
  "meshagent-agents~=0.0.20",
]

tools = [ 
  "meshagent-tools~=0.0.20",
]

cli = [
  "meshagent-cli~=0.0.21"
]

mcp-service = [
  "meshagent-cli~=0.0.21"
]
