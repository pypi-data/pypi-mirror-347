[project]
name = "mock-ads-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Xingjian Guo", email = "guoxj@oasalife.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=2.3.3",
    "pydantic>=2.11.4",
]

[project.scripts]
mock-ads-mcp = "mock_ads_mcp:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
