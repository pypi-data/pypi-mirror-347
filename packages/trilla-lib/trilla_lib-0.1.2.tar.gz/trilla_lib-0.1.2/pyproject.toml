[tool.poetry]
name = "trilla-lib"
version = "0.1.2"
description = ""
authors = ["Артур Лагунов <lagunovartur@inbox.ru>"]
license = "0.1.0"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
pydantic = ">=1.10.0,<2.0.0"
sentry-sdk = {extras = ["fastapi", "redis", "sqlalchemy", "aiohttp", 'celery'], version = "^2.26.1"}


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
