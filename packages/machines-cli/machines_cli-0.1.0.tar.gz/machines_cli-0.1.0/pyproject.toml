[project]
name = "machines-cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "pydantic>=2.11.4",
    "pydantic-settings>=2.9.1",
    "typer>=0.15.4",
]

[project.scripts]
lazycloud = "machines_cli.main:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
