# pyproject.toml
[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mypptpkg"  # must be globally unique on PyPI
version = "0.0.1"
description = "A demo package that runs a function on import"
authors = [{ name="Your Name", email="you@example.com" }]
readme = "README.md"
requires-python = ">=3.7"
license = {text = "MIT"}

