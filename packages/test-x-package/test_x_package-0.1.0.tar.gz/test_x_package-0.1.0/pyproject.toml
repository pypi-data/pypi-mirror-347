[project]
name = "test-x-package"
version = "0.1.0"
description = "Just a test package to develop ci jobs"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "ruff>=0.11.9",
]
