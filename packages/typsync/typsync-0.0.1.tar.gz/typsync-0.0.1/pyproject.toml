[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "typsync"
version = "0.0.1"
description = ""
readme = "README.md"
license = { file = "LICENSE" }
authors = [{ name = "daizutabi", email = "daizutabi@gmail.com" }]
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Science/Research",
  "Intended Audience :: Education",
  "Programming Language :: Python",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Documentation",
  "Topic :: Scientific/Engineering",
  "Topic :: Software Development :: Documentation",
  "Topic :: Text Processing :: Markup",
  "Operating System :: OS Independent",
  "License :: OSI Approved :: MIT License",
]
requires-python = ">=3.10"
keywords = [
  "jupyter",
  "notebook",
  "pdf",
  "publication",
  "academic",
  "visualization",
  "scientific",
  "research",
  "document",
]
dependencies = [
  "aiohttp>=3",
  "nbstore>=0.4.1",
  "pyzotero>=1.6",
  "rich>=13",
  "typer>=0.15",
  "watchfiles>=1",
]

[project.urls]
Documentation = "https://daizutabi.github.io/typsync/"
Source = "https://github.com/daizutabi/typsync"
Issues = "https://github.com/daizutabi/typsync/issues"

[project.scripts]
typsync = "typsync.cli:app"

[dependency-groups]
dev = [
  "ipykernel>=6",
  "matplotlib>=3.6",
  "pytest-clarity>=1",
  "pytest-cov>=6",
  "pytest-randomly>=3.16",
  "pytest-xdist>=3.6",
  "ruff>=0.11",
]
docs = ["mkdocs-material"]

[tool.pytest.ini_options]
addopts = ["--cov=typsync", "--cov-report=lcov:lcov.info", "--doctest-modules"]

[tool.coverage.report]
exclude_lines = ["no cov", "raise NotImplementedError", "if TYPE_CHECKING:"]
skip_covered = true

[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
select = ["ALL"]
unfixable = ["F401"]
ignore = [
  "A002",
  "ANN401",
  "D",
  "FBT001",
  "N802",
  "PGH003",
  "PLR0913",
  "PLR2004",
  "S603",
  "SIM102",
  "TRY003",
]

[tool.ruff.lint.per-file-ignores]
"*.ipynb" = ["ANN", "F403", "F405", "T201"]
"cli.py" = ["TC003"]
"tests/*" = ["ANN", "FBT", "S101"]

[tool.pyright]
include = ["src", "tests"]
strictDictionaryInference = true
strictListInference = true
strictSetInference = true
