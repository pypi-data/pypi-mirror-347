[project]
name = "btrip-mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "洒花", email = "zhangwenzhe.zwz@alibaba-inc.com" }
]
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.8.1",
]

[project.scripts]
btrip-mcp-server = "btrip_mcp_server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
