[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"


[project]
name = "mcp_weather_example"
version = "0.7.1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.28.1",
    "mcp>=1.6.0",
    "openai>=1.76.2",
    "requests>=2.32.3",
    "setuptools>=80.2.0",
    "twine>=6.1.0",
    "wheel>=0.45.1",
]
[project.scripts]
mcp_weather_example = "weather_mcp_server:main"

[tool.setuptools.packages.find]
where = ["."]
exclude =  ['Lib', 'Scripts']