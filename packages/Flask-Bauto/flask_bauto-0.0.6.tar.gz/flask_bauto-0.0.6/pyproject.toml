[build-system]
requires = ["setuptools","setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "Flask-Bauto"
version = "0.0.6"
readme = "README.md"
dependencies = [
  'Flask-FEFset',
  'Flask-UXFab',
  'Flask-SQLAlchemy'
]
