
[project]
name = "r2r-protocol"
version = "0.1.2"  # Version of the SDK
description = "Robot-to-Robot (R2R) Communication Protocol SDK in Python"
readme = "README.md"
requires-python = ">=3.7"
license = { text = "Apache 2.0" }
authors = [
    { name="Tech-Parivartan", email="techparivartan022@gmail.com" }
]
dependencies = []

[tool.setuptools.packages.find]
where = ["sdk/python"] # Looks for packages in the sdk/python directory

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"


