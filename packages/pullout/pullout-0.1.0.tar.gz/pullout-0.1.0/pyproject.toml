[project]
name = "pullout"
version = "0.1.0"
description = "Easy way to pull out values from a complex nested Python structure"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
authors = [
    { name="Aleksei Trankov", email="trankov@yandex.ru" }
]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
urls = { "Repository" = "https://gitverse.ru/trankov/pullout" }
