[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "PyMouPosTracker"
version = "0.1.0"
description = "A Python libary that tracks the x and y positisons of your mouse curosor"
readme = "README.md"
requires-python = ">=3.6"
license = {text = "MIT"}
authors = [
  { name="hlee100", email="henrylee1606@gmail.com" }
]
dependencies = [
  "pyautogui",
  "keyboard"
]

[project.urls]
Homepage = "https://github.com/hlee100/PyMouPosTracker"
