[project]
name = "bg-myproject"
version = "0.1.0"
dependencies = []

[project.optional-dependencies]
bgtrainer = ["bgtrainer<=0.1.0"]
bgoptimizer = ["bgoptimizer<=0.1.0"]

[tool.uv.workspace]
members = ["members/bgtrainer", "members/bgoptimizer"]

[tool.uv.sources]
trainer = { workspace = true }
optimizer = { workspace = true }

[tool.uv]
package = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["members"]

[tool.hatch.metadata]
allow-direct-references = true
