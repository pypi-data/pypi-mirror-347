from setuptools import setup, find_packages

setup(
    name="turingMachinePract",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # Add any dependencies your package needs here
        # For example:
        # 'numpy',
        # 'pandas',
    ],
)
