[project]
name = "dit-ml"
version = "0.1.0"
description = "DiT (Diffusion Transformer)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "timm>=1.0.15",
    "torch>=2.4.0",
]

[tool.setuptools]
packages = ["dit_ml"]

