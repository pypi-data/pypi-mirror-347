[tool.poetry]
name = "wpylib"
version = "0.0.12"
description = "A simple and easy-to-use library in python"
authors = ["Wgrape"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
