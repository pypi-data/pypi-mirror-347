[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "unicorner-face-detector"
version = "0.1.0"
description = "UNICORNER Coffee’s simple FaceDetector module (YuNet wrapper)"
readme = "README.md"
requires-python = ">=3.7"
authors = [{ name="Ruslan Kovalev", email="kovalyovruslan@gmail.com" }]
dependencies = [
  "opencv-python>=4.5"
]

[tool.setuptools.packages.find]
where = ["."]
include = ["unicorner_face_detector*"]
