[project]
name = "sentry-rate-limiter"
version = "1.0.6"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "kombu>=5.5.3",
]

[project.optional-dependencies]
extra = [
    "requests>=2.32.3",
    "sentry-sdk>=2.8.0"
]