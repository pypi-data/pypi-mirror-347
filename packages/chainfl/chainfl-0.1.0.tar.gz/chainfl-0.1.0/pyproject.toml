[tool.poetry]
name = "chainfl"
version = "0.1.0"
description = "Federated Learning + Blockchain Simulation Framework"
authors = ["Tymoteusz Miller <tymoteusz@example.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.10"
numpy = "^1.24"
scikit-learn = "^1.3"
cryptography = "^41.0"
pyyaml = "^6.0"

[tool.poetry.dev-dependencies]
pytest = "^7.4"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"