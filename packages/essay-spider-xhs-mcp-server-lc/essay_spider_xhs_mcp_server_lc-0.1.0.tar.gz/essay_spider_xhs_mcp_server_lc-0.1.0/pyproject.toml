[project]
name = "essay-spider-xhs-mcp-server-lc"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Melle_lichao", email = "lichao10060909@163.com" }
]
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.8.1",
]

[project.scripts]
essay-spider-xhs-mcp-server-lc = "essay_spider_xhs_mcp_server_lc:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
