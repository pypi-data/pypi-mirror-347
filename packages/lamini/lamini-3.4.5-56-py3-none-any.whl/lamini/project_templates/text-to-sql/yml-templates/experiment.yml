Experiment:
  experiment_name:
    value: experiment_name_placeholder
    description: "Name of the experiment."

memory_tuning:
  max_steps:
    value: 100
    description: "Maximum number of steps for memory tuning."
  learning_rate:
    value: 0.0003
    description: "Learning rate for memory tuning."
  max_gpus:
    value: 6
    description: "Maximum number of GPUs to use."
  max_nodes:
    value: 1
    description: "Maximum number of nodes to use."

pipeline:
  path: pipelines/default_pipeline.json