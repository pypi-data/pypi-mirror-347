[project]
name = "pathval"
version = "0.1.3"
description = "Validate file and directory paths for platform compatibility"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "pathvalidate==3.2.3",
]

[project.scripts]
pathval = "pathval.cli:main"
