[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[tool.blue]
line-length = 88
target-version = ['py37']
include = '\.pyi?$'

[tool.isort]
profile = "blue"
multi_line_output = 3
