[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pipex_loss"
version = "0.1.0"
description = "A PyTorch loss package with log-cosh and PIPEX loss"
readme = "README.md"
requires-python = ">=3.7"
license = {text = "MIT"}
authors = [
  { name = "Your Name", email = "you@example.com" }
]
dependencies = [
    "torch>=1.8",
    "pytorch-msssim"
]

[project.urls]
Homepage = "https://github.com/yourusername/pipex-loss"