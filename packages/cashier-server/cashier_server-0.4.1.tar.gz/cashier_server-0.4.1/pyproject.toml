[project]
name = "cashier-server"
version = "0.4.1"
description = "Cashier synchronization server in Python"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.12",
    "loguru>=0.7.3",
    "uvicorn>=0.34.2",
]

[project.scripts]
cashier-server-py = "main:main"
