[project]
name = "adb-mcp-server"
version = "1.0.0"
description = "A server implementation for handling ADB (Android Debug Bridge) communications using the Message Control Protocol (MCP)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.6.0",
]
[project.scripts]
adb-mcp-server="main:main"